import Foundation

// SpinRepository — single source of truth
// Reads from SwiftData cache, syncs from gRPC, writes back locally
struct SpinRepository {
    // TODO: inject SpinGRPCClient + SwiftData context
    // TODO: implement fetch, stream, mutate methods
}
