import Foundation

// StoryRepository — single source of truth
// Reads from SwiftData cache, syncs from gRPC, writes back locally
struct StoryRepository {
    // TODO: inject StoryGRPCClient + SwiftData context
    // TODO: implement fetch, stream, mutate methods
}
