import Foundation

// CircleRepository — single source of truth
// Reads from SwiftData cache, syncs from gRPC, writes back locally
struct CircleRepository {
    // TODO: inject CircleGRPCClient + SwiftData context
    // TODO: implement fetch, stream, mutate methods
}
