import SwiftData
import Foundation

// CircleStore — SwiftData persistence for offline-first circle data
@Model
final class CircleStore {
    var id: String = ""
    // TODO: persisted fields mirroring Circle domain model
    init() {}
}
